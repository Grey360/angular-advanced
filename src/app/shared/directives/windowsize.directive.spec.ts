import { WindowsizeDirective } from './windowsize.directive';

describe('WindowsizeDirective', () => {
  it('should create an instance', () => {
    const directive = new WindowsizeDirective();
    expect(directive).toBeTruthy();
  });
});
